# Picture Grid Quest

A browser-based picture puzzle game. Players swap image tiles to complete each picture. The game starts at a 4x4 grid, advances after three completions, then increases the grid size with one additional required completion at each level.

Open `index.html` directly or serve the folder with a static web server.
